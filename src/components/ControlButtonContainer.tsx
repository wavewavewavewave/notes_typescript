import {Root} from "../styles/ControlButtonStyle";
import {ControlButton} from "./ControlButton";


export const ControlButtonContainer = () => {
    return (
        <Root>
            <ControlButton name={'All'}/>
        </Root>
    )
}